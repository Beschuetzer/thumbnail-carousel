/// <reference types="react" />
import { ButtonProps } from "../../types";
type RestartButtonProps = {} & ButtonProps;
export declare const RestartButton: ({ childStyle, className, fillColor, onClick, }: RestartButtonProps) => import("react").JSX.Element;
export {};
